# TumuAI.net 网站健康评估报告

**评估日期**：2026-08-15
**评估方式**：线上实测（浏览器 e2e + Lighthouse 移动端 + curl 层面检查）+ 本地仓库代码抽查
**评估范围**：https://www.tumuai.net（中英双语全站）

---

## 总体结论

**网站整体健康度：良好（B+）。** 核心功能、SEO 基础、缓存策略、安全基线都在水准之上，无阻断性故障。翻页 bug（当日已修复，commit `cb786e0`）后未再发现功能性 P0。剩余问题集中在**分享传播链路**（og:image）和**双语 SEO 对称性**（hreflang），这两个直接影响流量获取，建议优先修。

| 维度 | 评分 | 一句话结论 |
|---|---|---|
| 功能正确性 | ✅ 优 | 核心路径全过（详见下文清单）|
| SEO | ⚠️ 95 | Lighthouse 100，但中英 hreflang 不对称 |
| 性能 | ✅ 优 | gzip ~200 kB，缓存策略专业 |
| 可访问性 | ⚠️ 82 | 6 项细节失分，同时拉低 AI 爬虫友好度 |
| 移动端 | ✅ 优 | 无溢出、交互正常 |
| 安全 | ✅ 良 | 无泄露、有鉴权；缺 CSP 纵深防御 |

---

## 问题清单（按优先级）

### P1 — 影响流量获取，建议本周修

**1. 工具详情页 og:image 是 data:URI，社交分享无卡片图**
- 现象：详情页 `<meta property="og:image" content="data:image/svg+xml,...">`
- 影响：微信/Telegram/X/Twitter 爬虫不支持 data:URI → 分享工具链接时不显示预览图。工具导航站靠分享传播，这是获客链路上的洞
- 修法：详情页 meta 生成时把 fallback 的 data:URI 换成站内 `og-image.png`（已存在，30.5 KB）；有真实 logo 的工具用 logo 绝对 URL

**2. 中文首页（/）缺 hreflang，中英页面不被搜索引擎关联**
- 现象：`/en` 有完整 3 个 alternate（zh-CN/en/x-default），`/` 一个都没有（不对称）
- 影响：Google 无法从中文页发现英文页 → 英文版 SEO 流量受损；也可能被判定重复内容
- 修法：prerender 脚本给中文页面注入与英文版对称的 hreflang 标签

### P2 — 建议排期修

**3. 英文版 Submit 表单 placeholder 中文残留**（"例如：StructuralGPT"等 4 处）——英文用户体验硬伤，改动小

**4. 可访问性 6 项失分（同时导致 Agentic Browsing 50 分）**
- 移动端汉堡按钮无 aria-label、排序下拉框无 label、页脚 GitHub 图标链接无名
- text-gray-500 对比度不足
- h1→h3 标题跳级
- 工具卡片 aria-label（"查看工具：X"）与可见文本不匹配 → **AI 爬虫（ChatGPT/Claude 引用你的站时）解析受阻**——2026 年 AI 引荐流量占比越来越高，这项值得修

**5. URL 状态不一致：search/sortBy 进 URL，但翻页页码和分类筛选不进**
- 刷新丢页码/筛选、无法分享带页码的链接、翻页内容对 SEO 不可见
- 修法：onPageChange 时 setSearchParams 同步 page 参数

**6. 无 Content-Security-Policy 头**——防 XSS 纵深防御，加一条 report-only 起步即可

**7. 海外工具 favicon 大量 404/429/504**（briq.com、maket.ai 等 10+ 个）——已有首字母 fallback 兜底，仅 console 噪音；可选修法：favicon 失败后缓存「已失败」标记避免重复请求

**8. 大陆访客深层风险：API 慢于 1500ms 时 fallback 直连 supabase.co**（大陆基本不通）→ 翻页/加载长时间转圈
- 这是翻页 bug 诊断时的附带发现；如果目标用户主要在大陆，根治方案是 fallback 改走自家 API 重试；若用户群偏海外则维持现状

---

## 已验证正常项（本次实测通过）

- **功能 e2e**：首页/列表/详情/Submit/About/搜索（BIM→17 结果）/分类筛选（交叉→2 结果）/排序切换/网格列表视图/语言切换（/en↔/）/登录弹窗（邮箱+密码）/翻页（中英文 1→2→3→4→回退，修复后稳定）
- **SEO**：sitemap.xml（220 URL，含 lastmod）、robots.txt、裸域→www 301、http→https 308、canonical、JSON-LD、首页 og 标签齐全、og-image.png 存在
- **性能**：Lighthouse Best Practices 100 / SEO 100；JS 带 hash 文件名 + `immutable` 缓存；HTML/API 分层 TTL + stale-while-revalidate；首屏 gzip ~200 kB
- **移动端**：375px 无水平溢出、汉堡菜单正常、翻页正常
- **安全**：无 service_role key 泄露（仅 anon key 公开，Supabase 设计使然）；admin-api 未授权返回 401；RLS 维护记录在仓库

---

## 建议修复顺序

1. og:image 修复（P1，改动小收益大）
2. hreflang 对称（P1，prerender 脚本一处改动）
3. i18n placeholder（P2，4 处字符串）
4. 可访问性 6 项（P2，尤其 aria-label 匹配 → 利于 AI 引荐流量）
5. URL 状态同步（P2）
6. CSP / favicon / supabase fallback（P2，看用户群定位决定）
